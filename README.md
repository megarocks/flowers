# Streams of Flowers and Bouquetes

### Running application with docker

Build image from Dockerfile
```console
> docker build -t megarocks-flowers .
```

Run application
```console
> cat test_input.txt | docker run -i megarocks-flowers
```
